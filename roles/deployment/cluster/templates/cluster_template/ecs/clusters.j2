{
"items":
[
  {
    "name" : "{{ cluster.name }}",
    "displayName": "{{ cluster.name }}",
    "version" : "DATA_SERVICES1",
    "fullVersion": "{{ products | cloudera.cluster.get_product_version('ECS') }}",
    "clusterType": "EXPERIENCE_CLUSTER"
  }
]
}